using UnityEngine;

public class KeyGateManager : MonoBehaviour
{
    bool hasKeyChurchToGraveyard;
    bool hasKeyChurchToFoggyMarsh;
    bool hasKeyChurchToBrackishShore;
    bool hasKeyGraveyardOne;
    bool hasKeyGraveyardTwo;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
